Modos:
--dry-run (preview sem gravar);

--update para atualizar perguntas existentes;

--dedupe-semantic opcional: usa embeddings para detectar e pular quase-duplicatas (com threshold configurável);

--compute-emb opcional: calcula embeddings somente para as novas entradas imediatamente (evita rodar compute_embeddings.py depois);